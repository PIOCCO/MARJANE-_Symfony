{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>Numbrt</th>
                <td>{{ order.numbrt }}</td>
            </tr>
            <tr>
                <th>Ordered</th>
                <td>{{ order.ordered ? order.ordered|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Shipped</th>
                <td>{{ order.shipped ? order.shipped|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Ship_to</th>
                <td>{{ order.shipTo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_order_index') }}">back to list</a>

    <a href="{{ path('app_order_edit', {'id': order.id}) }}">edit</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
