<form method="post" action="{{ path('app_web_user_delete', {'loginId': web_user.loginId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ web_user.loginId) }}">
    <button class="btn">Delete</button>
</form>
